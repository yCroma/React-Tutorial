import React from "react";

const Square = ({ index }) => {
  return (
    <button
      className="square"
      onClick={() => {
      }}
    >
      {value}
    </button>
  );
};

export default Square;
